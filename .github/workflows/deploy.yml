name: Deploy Wangke Java Service

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

concurrency:
  group: deploy-wangkejava-service
  cancel-in-progress: true

env:
  DEPLOY_HOST: 101.42.94.41
  DEPLOY_USER: root
  DEPLOY_PORT: "22"
  APP_DIR: /www/wwwroot/wangkejava
  APP_NAME: wangkejava.jar
  LEGACY_APP_NAME: ruoyi-admin.jar
  APP_PORT: "8007"
  SERVER_JAVA_HOME: /usr/lib/jvm/java-17-konajdk

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Build package
        shell: bash
        run: |
          set -euo pipefail
          mvn -B -pl ruoyi-admin -am package -DskipTests
          test -f "ruoyi-admin/target/$APP_NAME"

      - name: Prepare SSH
        shell: bash
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          cat > ~/.ssh/deploy_key <<'SSH_KEY'
          ${{ secrets.SERVER_SSH_KEY }}
          SSH_KEY
          grep -q "PRIVATE KEY" ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          : > ~/.ssh/known_hosts

      - name: Recover existing backend before upload
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-i ~/.ssh/deploy_key -p $DEPLOY_PORT -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ConnectTimeout=20 -o ConnectionAttempts=1 -o ServerAliveInterval=15 -o ServerAliveCountMax=4"
          timeout 3m ssh $SSH_OPTS "$DEPLOY_USER@$DEPLOY_HOST" "APP_DIR='$APP_DIR' APP_NAME='$APP_NAME' APP_PORT='$APP_PORT' JAVA_HOME='$SERVER_JAVA_HOME' bash -s" <<'REMOTE'
          set -euo pipefail

          if pgrep -f "$APP_DIR/$APP_NAME" >/dev/null 2>&1; then
            echo "backend process already running"
            exit 0
          fi

          if [ ! -f "$APP_DIR/$APP_NAME" ]; then
            echo "existing backend package not found, skip recovery"
            exit 0
          fi

          mkdir -p "$APP_DIR/logs" "$APP_DIR/uploadPath"
          LOG_FILE="$APP_DIR/logs/${APP_NAME%.jar}.log"
          JAVA_BIN=""
          if [ -x "$JAVA_HOME/bin/java" ]; then
            JAVA_BIN="$JAVA_HOME/bin/java"
          elif command -v java >/dev/null 2>&1; then
            JAVA_BIN="$(command -v java)"
          else
            echo "java runtime not found"
            exit 1
          fi

          get_mysql_root_password() {
            python3 - <<'PY'
          import os, re, subprocess, sys
          log = "/www/server/panel/logs/installed/mysql-8.4_1775550897.log"
          pattern = r"(?i)(?:password|passwd|pwd|root)[^\n\r:=]{0,30}[:=]\s*['\"]?([^'\"\s,;]+)"
          if not os.path.exists(log):
              sys.exit(1)
          text = open(log, errors="ignore").read()
          for match in re.finditer(pattern, text):
              password = match.group(1).strip()
              if not (3 <= len(password) <= 80):
                  continue
              env = os.environ.copy()
              env["MYSQL_PWD"] = password
              result = subprocess.run(["mysql", "-uroot", "-e", "select 1"], env=env, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
              if result.returncode == 0:
                  print(password)
                  sys.exit(0)
          sys.exit(1)
          PY
          }

          MYSQL_ROOT_PASSWORD="$(get_mysql_root_password || true)"
          if [ -n "$MYSQL_ROOT_PASSWORD" ]; then
            export SPRING_DATASOURCE_DRUID_MASTER_PASSWORD="$MYSQL_ROOT_PASSWORD"
          fi

          cd "$APP_DIR"
          nohup "$JAVA_BIN" \
            -Dname="$APP_NAME" \
            -Duser.timezone=Asia/Shanghai \
            -Xms512m \
            -Xmx1024m \
            -jar "$APP_DIR/$APP_NAME" \
            --ruoyi.profile="$APP_DIR/uploadPath" \
            --server.port="$APP_PORT" \
            > "$LOG_FILE" 2>&1 &

          for i in $(seq 1 45); do
            if ! pgrep -f "$APP_DIR/$APP_NAME" >/dev/null 2>&1; then
              echo "java process exited during recovery"
              tail -n 80 "$LOG_FILE" || true
              exit 1
            fi
            HTTP_CODE="$(curl -s -o /dev/null -w '%{http_code}' "http://127.0.0.1:$APP_PORT/" || true)"
            if [ "$HTTP_CODE" != "000" ]; then
              echo "existing backend recovered with http code $HTTP_CODE"
              exit 0
            fi
            sleep 2
          done

          echo "existing backend did not respond during recovery"
          tail -n 80 "$LOG_FILE" || true
          exit 1
          REMOTE

      - name: Upload backend package
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-i ~/.ssh/deploy_key -p $DEPLOY_PORT -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ConnectTimeout=20 -o ConnectionAttempts=1 -o ServerAliveInterval=15 -o ServerAliveCountMax=4"
          SCP_OPTS="-O -i ~/.ssh/deploy_key -P $DEPLOY_PORT -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ConnectTimeout=20 -o ConnectionAttempts=1 -o ServerAliveInterval=15 -o ServerAliveCountMax=4"
          RELEASE_DIR="$APP_DIR/releases/$GITHUB_SHA"
          PACKAGE_PATH="ruoyi-admin/target/$APP_NAME"

          retry() {
            local max="$1"
            shift
            local i
            for i in $(seq 1 "$max"); do
              if "$@"; then
                return 0
              fi
              [ "$i" = "$max" ] && return 1
              sleep $((i * 10))
            done
          }

          upload_source_archive() {
            timeout 4m ssh $SSH_OPTS "$DEPLOY_USER@$DEPLOY_HOST" "cat > '$RELEASE_DIR/backend-source.tgz'" < backend-source.tgz
          }

          echo "Creating remote release directory..."
          retry 6 timeout 90s ssh $SSH_OPTS "$DEPLOY_USER@$DEPLOY_HOST" "rm -rf '$RELEASE_DIR/source' && mkdir -p '$RELEASE_DIR' '$APP_DIR/logs' '$APP_DIR/uploadPath'"
          if [ -f sql/ry_20250522.sql ]; then
            echo "Uploading database bootstrap SQL..."
            retry 4 timeout 2m scp $SCP_OPTS sql/ry_20250522.sql "$DEPLOY_USER@$DEPLOY_HOST:$RELEASE_DIR/ry_20250522.sql"
          fi

          echo "Creating source archive..."
          git archive --format=tar.gz --output backend-source.tgz "$GITHUB_SHA"
          SOURCE_SHA="$(sha256sum backend-source.tgz | awk '{print $1}')"
          SOURCE_SIZE="$(stat -c%s backend-source.tgz)"

          echo "Uploading source archive..."
          retry 5 upload_source_archive

          echo "Building backend package on server..."
          timeout 35m ssh $SSH_OPTS "$DEPLOY_USER@$DEPLOY_HOST" "APP_DIR='$APP_DIR' APP_NAME='$APP_NAME' RELEASE_DIR='$RELEASE_DIR' SOURCE_SHA='$SOURCE_SHA' SOURCE_SIZE='$SOURCE_SIZE' JAVA_HOME='$SERVER_JAVA_HOME' bash -s" <<'REMOTE'
          set -euo pipefail
          ACTUAL_SIZE="$(stat -c%s "$RELEASE_DIR/backend-source.tgz")"
          ACTUAL_SHA="$(sha256sum "$RELEASE_DIR/backend-source.tgz" | awk '{print $1}')"
          if [ "$ACTUAL_SIZE" != "$SOURCE_SIZE" ] || [ "$ACTUAL_SHA" != "$SOURCE_SHA" ]; then
            echo "source verification failed: size=$ACTUAL_SIZE sha=$ACTUAL_SHA"
            exit 1
          fi

          if [ -x "$JAVA_HOME/bin/java" ]; then
            export JAVA_HOME="$JAVA_HOME"
            export PATH="$JAVA_HOME/bin:$PATH"
          fi

          if ! command -v mvn >/dev/null 2>&1; then
            if command -v yum >/dev/null 2>&1; then
              yum install -y maven
            elif command -v apt-get >/dev/null 2>&1; then
              apt-get update
              apt-get install -y maven
            else
              echo "maven not found and no supported package manager available"
              exit 1
            fi
          fi

          mkdir -p "$HOME/.m2"
          cat > "$HOME/.m2/settings.xml" <<'MAVEN_SETTINGS'
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
            <mirrors>
              <mirror>
                <id>aliyunmaven</id>
                <mirrorOf>*</mirrorOf>
                <name>Aliyun Maven</name>
                <url>https://maven.aliyun.com/repository/public</url>
              </mirror>
            </mirrors>
          </settings>
          MAVEN_SETTINGS

          rm -rf "$RELEASE_DIR/source"
          mkdir -p "$RELEASE_DIR/source"
          tar -xzf "$RELEASE_DIR/backend-source.tgz" -C "$RELEASE_DIR/source"
          cd "$RELEASE_DIR/source"
          MAVEN_OPTS="${MAVEN_OPTS:-} -Xmx768m" mvn -B -pl ruoyi-admin -am package -DskipTests
          test -f "ruoyi-admin/target/$APP_NAME"
          cp "ruoyi-admin/target/$APP_NAME" "$APP_DIR/$APP_NAME.tmp"
          mv "$APP_DIR/$APP_NAME.tmp" "$APP_DIR/$APP_NAME"
          REMOTE
          echo "RELEASE_DIR=$RELEASE_DIR" >> "$GITHUB_ENV"

      - name: Build release on server
        shell: bash
        run: |
          set -euo pipefail
          test -n "${RELEASE_DIR:-}"
          echo "Release package has been uploaded to $APP_DIR/$APP_NAME"

      - name: Ensure database
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-i ~/.ssh/deploy_key -p $DEPLOY_PORT -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ConnectTimeout=20 -o ConnectionAttempts=1 -o ServerAliveInterval=15 -o ServerAliveCountMax=4"
          timeout 3m ssh $SSH_OPTS "$DEPLOY_USER@$DEPLOY_HOST" "RELEASE_DIR='$RELEASE_DIR' bash -s" <<'REMOTE'
          set -euo pipefail

          get_mysql_root_password() {
            python3 - <<'PY'
          import os, re, subprocess, sys
          log = "/www/server/panel/logs/installed/mysql-8.4_1775550897.log"
          pattern = r"(?i)(?:password|passwd|pwd|root)[^\n\r:=]{0,30}[:=]\s*['\"]?([^'\"\s,;]+)"
          if not os.path.exists(log):
              sys.exit(1)
          text = open(log, errors="ignore").read()
          for match in re.finditer(pattern, text):
              password = match.group(1).strip()
              if not (3 <= len(password) <= 80):
                  continue
              env = os.environ.copy()
              env["MYSQL_PWD"] = password
              result = subprocess.run(["mysql", "-uroot", "-e", "select 1"], env=env, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
              if result.returncode == 0:
                  print(password)
                  sys.exit(0)
          sys.exit(1)
          PY
          }

          MYSQL_ROOT_PASSWORD="$(get_mysql_root_password || true)"
          if [ -z "$MYSQL_ROOT_PASSWORD" ]; then
            echo "mysql root password not detected, skip database bootstrap"
            exit 0
          fi

          DB_EXISTS="$(MYSQL_PWD="$MYSQL_ROOT_PASSWORD" mysql -uroot -N -e "show databases like 'ha'" || true)"
          if [ "$DB_EXISTS" != "ha" ]; then
            test -f "$RELEASE_DIR/ry_20250522.sql"
            MYSQL_PWD="$MYSQL_ROOT_PASSWORD" mysql -uroot -e "create database ha default character set utf8mb4 collate utf8mb4_general_ci"
            MYSQL_PWD="$MYSQL_ROOT_PASSWORD" mysql -uroot ha < "$RELEASE_DIR/ry_20250522.sql"
          fi
          REMOTE

      - name: Restart service
        shell: bash
        run: |
          set -euo pipefail
          SSH_OPTS="-i ~/.ssh/deploy_key -p $DEPLOY_PORT -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR -o ConnectTimeout=20 -o ConnectionAttempts=1 -o ServerAliveInterval=15 -o ServerAliveCountMax=4"
          timeout 6m ssh $SSH_OPTS "$DEPLOY_USER@$DEPLOY_HOST" "APP_DIR='$APP_DIR' APP_NAME='$APP_NAME' LEGACY_APP_NAME='$LEGACY_APP_NAME' APP_PORT='$APP_PORT' JAVA_HOME='$SERVER_JAVA_HOME' bash -s" <<'REMOTE'
          set -euo pipefail

          SERVICE_NAME="wangkejava"
          LOG_FILE="$APP_DIR/logs/${APP_NAME%.jar}.log"

          mkdir -p "$APP_DIR/logs" "$APP_DIR/uploadPath"

          cat > "$APP_DIR/run-$SERVICE_NAME.sh" <<'RUNNER'
          #!/usr/bin/env bash
          set -euo pipefail

          APP_DIR="${APP_DIR:-/www/wwwroot/wangkejava}"
          APP_NAME="${APP_NAME:-wangkejava.jar}"
          APP_PORT="${APP_PORT:-8007}"
          JAVA_HOME="${JAVA_HOME:-/usr/lib/jvm/java-17-konajdk}"
          LOG_FILE="$APP_DIR/logs/${APP_NAME%.jar}.log"

          JAVA_BIN=""
          if [ -x "$JAVA_HOME/bin/java" ]; then
            JAVA_BIN="$JAVA_HOME/bin/java"
          elif command -v java >/dev/null 2>&1; then
            JAVA_BIN="$(command -v java)"
          else
            echo "java runtime not found" >> "$LOG_FILE"
            exit 1
          fi

          get_mysql_root_password() {
            python3 - <<'PY'
          import os, re, subprocess, sys
          log = "/www/server/panel/logs/installed/mysql-8.4_1775550897.log"
          pattern = r"(?i)(?:password|passwd|pwd|root)[^\n\r:=]{0,30}[:=]\s*['\"]?([^'\"\s,;]+)"
          if not os.path.exists(log):
              sys.exit(1)
          text = open(log, errors="ignore").read()
          for match in re.finditer(pattern, text):
              password = match.group(1).strip()
              if not (3 <= len(password) <= 80):
                  continue
              env = os.environ.copy()
              env["MYSQL_PWD"] = password
              result = subprocess.run(["mysql", "-uroot", "-e", "select 1"], env=env, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
              if result.returncode == 0:
                  print(password)
                  sys.exit(0)
          sys.exit(1)
          PY
          }

          MYSQL_ROOT_PASSWORD="$(get_mysql_root_password || true)"
          if [ -n "$MYSQL_ROOT_PASSWORD" ]; then
            export SPRING_DATASOURCE_DRUID_MASTER_PASSWORD="$MYSQL_ROOT_PASSWORD"
          fi

          cd "$APP_DIR"
          exec "$JAVA_BIN" \
            -Dname="$APP_NAME" \
            -Duser.timezone=Asia/Shanghai \
            -Xms512m \
            -Xmx1024m \
            -jar "$APP_DIR/$APP_NAME" \
            --ruoyi.profile="$APP_DIR/uploadPath" \
            --server.port="$APP_PORT" \
            >> "$LOG_FILE" 2>&1
          RUNNER
          chmod +x "$APP_DIR/run-$SERVICE_NAME.sh"

          cat > "/etc/systemd/system/$SERVICE_NAME.service" <<UNIT
          [Unit]
          Description=Wangke Java Service
          After=network-online.target mysql.service mysqld.service redis.service
          Wants=network-online.target

          [Service]
          Type=simple
          WorkingDirectory=$APP_DIR
          Environment=APP_DIR=$APP_DIR
          Environment=APP_NAME=$APP_NAME
          Environment=APP_PORT=$APP_PORT
          Environment=JAVA_HOME=$JAVA_HOME
          ExecStart=$APP_DIR/run-$SERVICE_NAME.sh
          Restart=always
          RestartSec=5
          SuccessExitStatus=143

          [Install]
          WantedBy=multi-user.target
          UNIT

          if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
            systemctl stop "$SERVICE_NAME.service" >/dev/null 2>&1 || true
          fi

          for app in "$APP_NAME" "$LEGACY_APP_NAME"; do
            [ -n "$app" ] || continue
            PID="$(pgrep -f "$APP_DIR/$app" || true)"
            if [ -n "$PID" ]; then
              kill -TERM $PID || true
            fi
          done

          for i in $(seq 1 30); do
            RUNNING=""
            for app in "$APP_NAME" "$LEGACY_APP_NAME"; do
              [ -n "$app" ] || continue
              if pgrep -f "$APP_DIR/$app" >/dev/null 2>&1; then
                RUNNING=1
              fi
            done
            [ -z "$RUNNING" ] && break
            sleep 1
          done

          if pgrep -f "$APP_DIR/$APP_NAME" >/dev/null 2>&1 || pgrep -f "$APP_DIR/$LEGACY_APP_NAME" >/dev/null 2>&1; then
            echo "old java process is still running"
            exit 1
          fi

          if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
            systemctl daemon-reload
            systemctl enable "$SERVICE_NAME.service"
            systemctl restart "$SERVICE_NAME.service"
          else
            nohup "$APP_DIR/run-$SERVICE_NAME.sh" >/dev/null 2>&1 &
          fi

          for i in $(seq 1 60); do
            if ! pgrep -f "$APP_DIR/$APP_NAME" >/dev/null 2>&1; then
              if command -v systemctl >/dev/null 2>&1 && systemctl is-failed --quiet "$SERVICE_NAME.service"; then
                systemctl status "$SERVICE_NAME.service" --no-pager || true
              fi
              echo "java process exited"
              tail -n 120 "$LOG_FILE" || true
              exit 1
            fi

            HTTP_CODE="$(curl -s -o /dev/null -w '%{http_code}' "http://127.0.0.1:$APP_PORT/" || true)"
            if [ "$HTTP_CODE" != "000" ]; then
              echo "service started with http code $HTTP_CODE"
              exit 0
            fi
            sleep 2
          done

          echo "service did not respond on port $APP_PORT"
          tail -n 120 "$LOG_FILE" || true
          exit 1
          REMOTE
